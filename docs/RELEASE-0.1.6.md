# WinToolbox 0.1.6

“设置 → 模型 → 阿里云百炼 API”新增本机用量统计，显示今日、本月和累计请求数、接口返回的 Token 数、语音发送时长及失败次数；可展开查看本月各模型。

统计从本版本开始保存在 `engine.sqlite3`，备份和迁移会随应用数据一起处理。只保存计数、模型和操作类型，不保存提示词、回答、音频或密钥。流式百炼请求按照官方接口启用 Token 用量返回；实时语音按实际发送给服务的音频时长统计。

这不是阿里云账户账单、余额或历史用量回填。账户中其他程序产生的调用不会出现在这里，最终费用以阿里云控制台为准。
