# 0.1.17

修复 FN Connect 全局 TUN 开启后的连接绕行和认证恢复。认证 HTTP 与 WebSocket 共用建立会话前解析的固定入口地址，保留原域名 SNI 与证书校验；TUN 排除这些地址，避免 DNS fake-IP 或入口解析变化造成回环。登录后重建携带 Cookie 的认证连接，保活认证带当前 si。DNS 改走家中路由器 TCP 解析。

失败后连接按钮可点击并提示缺少密码；提交失败保留输入便于重试，成功连接才清空。新增随时可用的重置连接，断开不受其他任务禁用；TUN 状态检查进程存活。

验证：NAS 直连 52:22 可达；原生隧道三次 SSH 握手和保活通过；完整全局 TUN 在开始、65 秒、130 秒的三轮测试均取得 52 SSH banner 与 HTTPS 响应；最后 connected，停止后 stopped。八项针对性后端与退出回归通过，桌面构建成功。
