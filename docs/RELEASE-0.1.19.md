# 0.1.19

回家 VPN 记住 TUN 开关，重新连接后恢复；断线和退出时撤销路由，手动关闭才清除开启偏好。
首次授权安装专用 Windows 服务，后续开关无需重复 UAC。工具箱继续以普通权限运行。

验证：12 项相关测试通过。真实 FN Connect 全局模式连续两次启停均未调用提权启动器，52 服务器 SSH 与 HTTPS 均成功；模拟客户端心跳停止后，服务按期撤销 TUN。服务只接受限定参数，安装目录仅管理员和系统可写。
